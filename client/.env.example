# Tunnel Client Configuration
# Copy this file to .env and customize as needed

# Tunnel server WebSocket URL (using WSS on port 443)
TUNNEL_SERVER=wss://easypod.cloud/tunnel

# Alternative configurations:
# For local development:
# TUNNEL_SERVER=ws://localhost:8080/tunnel

# For non-SSL on port 80:
# TUNNEL_SERVER=ws://easypod.cloud/tunnel

# Default subdomain (leave empty for random)
# Can be overridden by CLI argument
SUBDOMAIN=

# Example with custom subdomain:
# SUBDOMAIN=myapp

# Local server port
# Default: 3000
LOCAL_PORT=3000

# Local server host
# Default: localhost
LOCAL_HOST=localhost

# Example configurations:
# LOCAL_PORT=8080
# LOCAL_HOST=127.0.0.1
